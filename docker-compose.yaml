services:
  # 服务端（也扫描本地网络）
  home-presence:
    build: .
    command: ["-role", "both", "-home", "家庭A", "-port", "8080"]
    volumes:
      - ./data:/data
    restart: unless-stopped
    network_mode: host

  # 扫描器（可选，在其他家庭的机器上运行）
  # scanner:
  #   build: .
  #   command: ["-role", "scanner", "-home", "家庭B", "-central", "http://10.8.0.2:8080"]
  #   restart: unless-stopped
  #   network_mode: host
